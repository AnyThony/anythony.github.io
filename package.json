{
  "name": "portfolio",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "dependencies": {
    "concurrently": "^5.2.0"
  },
  "devDependencies": {},
  "scripts": {
    "dev": "concurrently --kill-others \"uni build && live-server --WATCH=/build --mount=/:%cd%/build --wait=200\" \"uni watch\"",
    "build": "uni build",
    "push": "git subtree push --prefix=build origin master && git push origin src"
  },
  "author": "",
  "license": "ISC"
}
